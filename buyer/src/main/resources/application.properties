spring.application.name=buyer

# Server Configuration
server.port=8082

# MongoDB Configuration (MongoDB Atlas)
# Database: mystara_seller (shared with seller for products/themes/purchase_requests)
# Collections accessed: products (read-only), themes (read-only), purchase_requests (read/write)
# Note: Buyer service accesses seller database for products/themes and purchase_requests
# TODO: Refactor to use separate buyer database and call Seller Service API for products/themes
spring.data.mongodb.uri=mongodb+srv://mystara:mystara@cluster0.4hocdmk.mongodb.net/mystara_seller?appName=Cluster0
spring.data.mongodb.database=mystara_seller

# Application Configuration
spring.data.mongodb.auto-index-creation=true

# Eureka Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true

# JWT Configuration (must match gateway)
jwt.secret=1158eb6472e9a1de08ab854d11482848
jwt.expiration=86400000
