spring.application.name=api-gateway

# Server Configuration
server.port=8089

# Eureka Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true

# Gateway Configuration
spring.cloud.gateway.discovery.locator.enabled=true
spring.cloud.gateway.discovery.locator.lower-case-service-id=true

# CORS Configuration - Handled by CorsConfig.java
# Note: Using CorsWebFilter bean instead of properties for better control

# Actuator Configuration
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
# Note: management.endpoint.gateway.access is not needed if endpoints are exposed via exposure.include

# JWT Configuration (must match buyer/seller services)
jwt.secret=1158eb6472e9a1de08ab854d11482848
jwt.expiration=86400000

razorpay.key.id=rzp_live_Fk90kGBnnWdkcF
razorpay.key.secret=KXtWDNSegOd4wnHtpnYEL8pS
